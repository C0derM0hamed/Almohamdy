@extends('layouts.app')
@section('title', __('legal_claims.title'))
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4 mb-1">{{ __('legal_claims.title') }}</h1><p class="text-muted mb-0">{{ __('legal_claims.scope') }}</p></div><a class="btn btn-primary" href="{{ route('modules.legal-claims.create') }}"><i class="bi bi-plus-lg"></i> {{ __('legal_claims.create') }}</a></div>
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body row g-3 align-items-end"><div class="col-md-3"><label class="form-label">{{ __('legal_claims.patient_name') }}</label><input class="form-control" name="patient_name" value="{{ $filters['patient_name'] }}"></div><div class="col-md-2"><label class="form-label">{{ __('legal_claims.mobile') }}</label><input class="form-control" name="mobile" value="{{ $filters['mobile'] }}"></div><div class="col-md-2"><label class="form-label">{{ __('legal_claims.status') }}</label><select class="form-select" name="status_id"><option value="">{{ __('legal_claims.all') }}</option>@foreach($statuses as $status)<option value="{{ $status->id }}" @selected($filters['status_id'] === $status->id)>{{ $status->name_ar }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">{{ __('legal_claims.from') }}</label><input class="form-control" type="date" name="from" value="{{ $filters['from'] }}"></div><div class="col-md-2"><label class="form-label">{{ __('legal_claims.to') }}</label><input class="form-control" type="date" name="to" value="{{ $filters['to'] }}"></div><div class="col-md-1"><button class="btn btn-primary w-100" title="{{ __('legal_claims.search') }}"><i class="bi bi-search"></i></button></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ __('legal_claims.date') }}</th><th>{{ __('legal_claims.patient_name') }}</th><th>{{ __('legal_claims.file_number') }}</th><th>{{ __('legal_claims.mobile') }}</th><th>{{ __('legal_claims.amount_rest') }}</th><th>{{ __('legal_claims.status') }}</th><th></th></tr></thead><tbody>@forelse($claims as $claim)<tr><td dir="ltr">{{ date('Y-m-d', (int) $claim->date) }}</td><td>{{ $claim->patient_name }}</td><td>{{ $claim->file_number }}</td><td dir="ltr">{{ $claim->liable_mobile }}</td><td>{{ $claim->amount_rest }}</td><td>{{ $claim->status_name_ar ?: $claim->status }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.legal-claims.show', $claim->id) }}" title="{{ __('legal_claims.open') }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-5">{{ __('legal_claims.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $claims->links() }}</div></div>
@endsection
