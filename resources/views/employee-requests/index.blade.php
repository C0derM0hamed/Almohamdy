@extends('layouts.app')
@section('title',$config['title'])
@section('content')<div class="d-flex justify-content-between align-items-end mb-4"><div><h1 class="h4">{{ $config['title'] }}</h1><p class="text-muted">{{ __('employee_requests.scope') }}</p></div><a class="btn btn-primary" href="{{ route('modules.employee-requests.create',$type) }}"><i class="bi bi-plus-lg"></i> {{ __('employee_requests.create') }}</a></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ __('employee_requests.employee') }}</th><th>{{ __('employee_requests.date') }}</th><th>{{ __('employee_requests.reason') }}</th><th>{{ __('employee_requests.branch_status') }}</th><th>{{ __('employee_requests.hr_status') }}</th><th></th></tr></thead><tbody>@forelse($requests as $row)<tr><td>{{ $row->hr_username }} - {{ $row->hr_first_name }} {{ $row->hr_last_name }}</td><td dir="ltr">{{ date('Y-m-d H:i',(int)$row->date) }}</td><td>{{ $row->reason }}</td><td>{{ $row->branch_reply?->status_name_ar ?: __('employee_requests.pending') }}</td><td>{{ $row->hr_reply?->status_name_ar ?: __('employee_requests.pending') }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.employee-requests.show',[$type,$row->id]) }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">{{ __('employee_requests.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $requests->links() }}</div></div>@endsection
