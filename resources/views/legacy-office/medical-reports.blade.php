@extends('layouts.app')
@section('title','اعتماد التقارير الطبية')
@section('content')
<h1 class="h4 mb-4">اعتماد التقارير الطبية</h1>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif
<div class="row g-3 mb-4">@foreach(['total'=>'الإجمالي','pending'=>'معلق','approved'=>'تمت الموافقة','rejected'=>'عدم الموافقة'] as $key=>$label)<div class="col-md-3"><div class="card border-0 shadow-sm"><div class="card-body"><div class="text-muted">{{$label}}</div><div class="fs-3">{{(int)$summary->$key}}</div></div></div></div>@endforeach</div>
@include('legacy-office.partials.filters',['filterDecisions'=>$decisions])
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>التاريخ</th><th>اسم المريض</th><th>رقم الملف</th><th>الطبيب</th><th>الحالة</th><th>الإجراء</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{$record->created_at}}</td><td>{{$record->patient_name}}</td><td>{{$record->file_number}}</td><td>{{$record->doctor}}</td><td>{{$record->manager_reply==1?'تمت الموافقة':($record->manager_reply==2?'مرفوض':'معلق')}}</td><td><a target="_blank" class="btn btn-sm btn-outline-secondary" href="{{route('modules.legacy-office.medical-reports.pdf',$record->id)}}">PDF</a> <button class="btn btn-sm btn-dark" data-bs-toggle="modal" data-bs-target="#report-{{$record->id}}">قرار</button></td></tr><div class="modal fade" id="report-{{$record->id}}" tabindex="-1"><div class="modal-dialog"><form method="post" action="{{route('modules.legacy-office.medical-reports.decision',$record->id)}}" class="modal-content">@csrf @method('patch')<div class="modal-header"><h2 class="h5">إضافة القرار</h2></div><div class="modal-body"><select class="form-select mb-3" name="manager_reply">@foreach($decisions as $decision)<option value="{{$decision->id}}">{{$decision->name_ar}}</option>@endforeach</select><input class="form-control" name="manager_reply_reason" placeholder="كتابة السبب"></div><div class="modal-footer"><button class="btn btn-primary">حفظ</button></div></form></div></div>@empty<tr><td colspan="6" class="text-center py-5 text-muted">لا توجد نتائج</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-transparent">{{$records->links()}}</div></div>
@endsection
