@extends('layouts.app')
@section('title','إشعار حالة الموافقة على تغطية الخدمات الطبية')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h4">إشعار حالة الموافقة على تغطية الخدمات الطبية</h1><button class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#coverage-create">إضافة إشعار</button></div>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif @if($errors->any())<div class="alert alert-danger">{{$errors->first()}}</div>@endif
<div class="collapse mb-4" id="coverage-create"><form method="post" action="{{route('modules.legacy-office.coverage.store')}}" class="card card-body border-0 shadow-sm">@csrf @include('legacy-office.partials.coverage-form',['record'=>null])<button class="btn btn-primary mt-3">حفظ</button></form></div>
@include('legacy-office.partials.filters',['filterTypes'=>$types])
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>تاريخ الإصدار</th><th>اسم المريض</th><th>رقم الملف الطبي</th><th>نوع الإشعار</th><th>أرسل إلى</th><th>اعتماد الإشعار</th><th>PDF</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{is_numeric($record->date)?date('Y-m-d H:i',(int)$record->date):$record->date}}</td><td>{{$record->patient_name}}</td><td>{{$record->file_number}}</td><td>{{$types->firstWhere('id',$record->memo_types_id)?->name_ar}}</td><td><span class="badge bg-secondary">{{$record->recipient_count}}</span></td><td>{{$record->activated_at?$record->activated_at:'غير معتمد'}} <button class="btn btn-sm btn-dark" data-bs-toggle="modal" data-bs-target="#coverage-{{$record->id}}">اعتماد</button></td><td><a target="_blank" class="btn btn-sm btn-outline-secondary" href="{{route('modules.legacy-office.coverage.pdf',$record->id)}}">PDF</a></td></tr><div class="modal fade" id="coverage-{{$record->id}}" tabindex="-1"><div class="modal-dialog modal-lg"><form method="post" action="{{route('modules.legacy-office.coverage.update',$record->id)}}" class="modal-content">@csrf @method('put')<div class="modal-header"><h2 class="h5">اعتماد الإشعار</h2></div><div class="modal-body">@include('legacy-office.partials.coverage-form',['record'=>$record])</div><div class="modal-footer"><button class="btn btn-primary">اعتماد</button></div></form></div></div>@empty<tr><td colspan="7" class="text-center text-muted py-5">لا توجد نتائج</td></tr>@endforelse</tbody></table></div><div class="card-footer bg-transparent">{{$records->links()}}</div></div>
@endsection
