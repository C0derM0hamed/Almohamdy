<section class="dda-page-hero" aria-labelledby="ddaPageTitle">
    <div>
        <h1 id="ddaPageTitle">{{ $title }}</h1>
        @if (! empty($subtitle))
            <p>{{ $subtitle }}</p>
        @endif
    </div>
    <div class="dda-page-hero-art" aria-hidden="true"></div>
</section>
