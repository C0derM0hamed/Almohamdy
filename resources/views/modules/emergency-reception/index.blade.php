@extends('layouts.app')
@section('title', $definition['title'])
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h4 mb-1">{{ $definition['title'] }}</h1><p class="text-muted mb-0">بيانات الفرع الحالي والشركة الحالية فقط</p></div><a class="btn btn-primary" href="{{ route('modules.emergency-reception.create', $type) }}"><i class="bi bi-plus-lg"></i> إضافة</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body row g-3 align-items-end"><div class="col-md-2"><label class="form-label">من تاريخ</label><input class="form-control" type="date" name="from" value="{{ $filters['from'] }}"></div><div class="col-md-2"><label class="form-label">إلى تاريخ</label><input class="form-control" type="date" name="to" value="{{ $filters['to'] }}"></div>@if($definition['status'])<div class="col-md-2"><label class="form-label">الحالة</label><select class="form-select" name="status"><option value="">الكل</option><option value="0" @selected($filters['status']==='0')>معلق</option><option value="1" @selected($filters['status']==='1')>مكتمل/مصادق</option><option value="2" @selected($filters['status']==='2')>مرفوض</option></select></div>@endif<div class="col-md-2"><label class="form-label">المدخل</label><select class="form-select" name="user_id"><option value="">الكل</option>@foreach($lookups['users'] as $user)<option value="{{ $user->hr_id }}" @selected($filters['user_id']==$user->hr_id)>{{ $user->hr_first_name }} {{ $user->hr_last_name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">رقم الهوية / بحث</label><input class="form-control" name="search" value="{{ $filters['search'] }}"></div><div class="col-md-1"><button class="btn btn-primary w-100"><i class="bi bi-search"></i></button></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr>@foreach($definition['columns'] as $label)<th>{{ $label }}</th>@endforeach<th>الإجراءات</th></tr></thead><tbody>@forelse($records as $record)<tr>@foreach($definition['columns'] as $field => $label)<td>{{ in_array($field, ['date','date_of_death','escape_date'], true) && is_numeric($record->$field) ? date('Y-m-d', (int)$record->$field) : $record->$field }}</td>@endforeach<td class="text-nowrap"><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.emergency-reception.show', [$type, $record->id]) }}"><i class="bi bi-eye"></i></a> <a class="btn btn-sm btn-outline-danger" href="{{ route('modules.emergency-reception.pdf', [$type, $record->id]) }}"><i class="bi bi-file-pdf"></i></a></td></tr>@empty<tr><td colspan="{{ count($definition['columns'])+1 }}" class="text-center text-muted py-5">لا توجد بيانات</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></div>
@endsection
