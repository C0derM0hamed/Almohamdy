@extends('layouts.app')
@section('title', $direction === 'incoming' ? __('transferal.incoming') : __('transferal.outgoing'))
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4 mb-1">{{ $direction === 'incoming' ? __('transferal.incoming') : __('transferal.outgoing') }}</h1><p class="text-muted mb-0">{{ __('transferal.scope') }}</p></div>@if($direction === 'outgoing')<a class="btn btn-primary" href="{{ route('modules.transferal.create') }}"><i class="bi bi-plus-lg"></i> {{ __('transferal.create') }}</a>@endif</div>
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body row g-3 align-items-end"><div class="col-md-4"><label class="form-label">{{ __('transferal.file_number') }}</label><input class="form-control" name="file_number" value="{{ $filters['file_number'] }}"></div><div class="col-md-3"><label class="form-label">{{ __('transferal.from') }}</label><input class="form-control" type="date" name="from" value="{{ $filters['from'] }}"></div><div class="col-md-3"><label class="form-label">{{ __('transferal.to') }}</label><input class="form-control" type="date" name="to" value="{{ $filters['to'] }}"></div><div class="col-md-2"><button class="btn btn-primary w-100" type="submit"><i class="bi bi-search"></i> {{ __('transferal.search') }}</button></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ __('transferal.date') }}</th><th>{{ __('transferal.patient') }}</th><th>{{ __('transferal.file_number') }}</th><th>{{ $direction === 'incoming' ? __('transferal.source') : __('transferal.target') }}</th><th>{{ __('transferal.status') }}</th><th></th></tr></thead><tbody>@forelse($transfers as $item)<tr><td dir="ltr">{{ date('Y-m-d H:i', (int)$item->date) }}</td><td>{{ $item->patient_name }}</td><td>{{ $item->file_number }}</td><td>{{ $direction === 'incoming' ? $item->source_name_ar : $item->target_name_ar }}</td><td>@if($item->refusal){{ __('transferal.refused') }}@elseif($item->receive){{ __('transferal.received') }}@elseif($item->approval){{ __('transferal.approved') }}@elseif($item->confirm){{ __('transferal.confirmed') }}@else{{ __('transferal.new') }}@endif</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.transferal.show', $item->id) }}" title="{{ __('transferal.open') }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">{{ __('transferal.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $transfers->links() }}</div></div>
@endsection
