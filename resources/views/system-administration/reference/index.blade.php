@extends('layouts.app')
@section('title', __('system_administration.reference.'.$spec['title']))
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4 mb-1">{{ __('system_administration.reference.'.$spec['title']) }}</h1><p class="text-muted mb-0">{{ __('system_administration.reference.scope') }}</p></div><a class="btn btn-primary" href="{{ route('modules.system-admin.reference.create', $type) }}"><i class="bi bi-plus-lg"></i> {{ __('system_administration.reference.create') }}</a></div>
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body d-flex gap-2"><input class="form-control" name="search" value="{{ $search }}" placeholder="{{ __('system_administration.reference.search') }}"><button class="btn btn-outline-primary"><i class="bi bi-search"></i></button></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>#</th><th>{{ __('system_administration.reference.name_ar') }}</th><th>{{ __('system_administration.reference.name_en') }}</th><th>{{ __('system_administration.reference.status') }}</th><th></th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row->id }}</td><td>{{ $row->name_ar ?? '' }}</td><td>{{ $row->name_en ?? '' }}</td><td>{{ isset($row->publish) && $row->publish ? __('system_administration.reference.published') : __('system_administration.reference.unpublished') }}</td><td class="text-nowrap"><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.system-admin.reference.edit', [$type, $row->id]) }}" title="{{ __('system_administration.reference.edit') }}"><i class="bi bi-pencil"></i></a>@if(isset($row->publish))<form class="d-inline" method="post" action="{{ route('modules.system-admin.reference.publish', [$type, $row->id]) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary" title="{{ __('system_administration.reference.toggle') }}"><i class="bi bi-eye"></i></button></form>@endif<form class="d-inline" method="post" action="{{ route('modules.system-admin.reference.destroy', [$type, $row->id]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" title="{{ __('system_administration.reference.delete') }}"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">{{ __('system_administration.reference.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $rows->links() }}</div></div>
@endsection
