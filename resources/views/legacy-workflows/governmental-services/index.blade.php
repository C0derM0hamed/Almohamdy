@extends('layouts.app')
@section('title', 'الخدمات الحكومية')
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4 mb-1">الخدمات الحكومية</h1><p class="text-muted mb-0">طلبات الفحوصات والخدمات المرتبطة بمنصة صحة</p></div><a class="btn btn-primary" href="{{ route('modules.governmental-services.create') }}"><i class="bi bi-plus-lg"></i> طلب جديد</a></div>
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body row g-3 align-items-end"><div class="col-md-2"><label class="form-label">من تاريخ</label><input class="form-control" type="date" name="from" value="{{ $filters['from'] }}"></div><div class="col-md-2"><label class="form-label">إلى تاريخ</label><input class="form-control" type="date" name="to" value="{{ $filters['to'] }}"></div><div class="col-md-3"><label class="form-label">الحالة</label><select class="form-select" name="status"><option value="">الكل</option><option value="0" @selected($filters['status']==='0')>معلق</option>@foreach($statuses as $status)<option value="{{ $status->id }}" @selected($filters['status']==(string)$status->id)>{{ $status->name_ar }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">نوع الخدمة</label><select class="form-select" name="service_id"><option value="">الكل</option>@foreach($types as $type)<option value="{{ $type->id }}" @selected($filters['service_id']==(string)$type->id)>{{ $type->name_ar }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">الهوية/رقم الملف</label><input class="form-control" name="identity" value="{{ $filters['identity'] }}"></div><div class="col-12 d-flex justify-content-end gap-2"><a class="btn btn-outline-secondary" href="{{ route('modules.governmental-services.index') }}">استعادة</a><button class="btn btn-primary"><i class="bi bi-search"></i> بحث</button></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>التاريخ</th><th>اسم المراجع</th><th>رقم الملف</th><th>رقم الهوية</th><th>نوع الخدمة</th><th>الحالة</th><th>الخط الزمني</th><th>النموذج</th></tr></thead><tbody>@forelse($records as $record)<tr><td dir="ltr">{{ date('Y-m-d H:i',(int)$record->date) }}</td><td>{{ $record->pateint }}</td><td>{{ $record->file_number }}</td><td>{{ $record->id_no }}</td><td>{{ $record->service_name }}</td><td>{{ $record->status_name ?: ((int)$record->status===0?'معلق':$record->status) }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.governmental-services.show',$record->id) }}"><i class="bi bi-clock-history"></i></a></td><td><a class="btn btn-sm btn-outline-danger" href="{{ route('modules.governmental-services.pdf',$record->id) }}"><i class="bi bi-file-earmark-pdf"></i> PDF</a></td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-5">لا توجد سجلات</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></div>
@endsection
