@extends('layouts.app')
@section('title', $type === 'manual' ? __('admission_calculator.manual') : __('admission_calculator.standard'))
@section('content')<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4">{{ $type === 'manual' ? __('admission_calculator.manual') : __('admission_calculator.standard') }}</h1><p class="text-muted">{{ __('admission_calculator.scope') }}</p></div><a class="btn btn-primary" href="{{ route('modules.admission-calculator.create',$type) }}"><i class="bi bi-plus-lg"></i> {{ __('admission_calculator.create') }}</a></div><form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body"><div class="row g-3 align-items-end"><div class="col-md-5"><label class="form-label">{{ __('admission_calculator.file_number') }}</label><input class="form-control" name="file_number" value="{{ $filters['file_number'] }}"></div><div class="col-md-3"><label class="form-label">{{ __('admission_calculator.from') }}</label><input class="form-control" type="date" name="from" value="{{ $filters['from'] }}"></div><div class="col-md-3"><label class="form-label">{{ __('admission_calculator.to') }}</label><input class="form-control" type="date" name="to" value="{{ $filters['to'] }}"></div><div class="col-md-1"><button class="btn btn-primary" type="submit" title="{{ __('admission_calculator.search') }}"><i class="bi bi-search"></i></button></div></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>{{ __('admission_calculator.date') }}</th><th>{{ __('admission_calculator.patient') }}</th><th>{{ __('admission_calculator.file_number') }}</th><th>{{ __('admission_calculator.days') }}</th><th>{{ __('admission_calculator.room') }}</th><th></th></tr></thead><tbody>@forelse($records as $item)<tr><td dir="ltr">{{ date('Y-m-d H:i',(int)$item->date) }}</td><td>{{ $item->patient_name }}</td><td>{{ $item->file_number }}</td><td>{{ $item->days }}</td><td>{{ $item->room }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-calculator.show',[$type,$item->id]) }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">{{ __('admission_calculator.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $records->links() }}</div></div>@endsection
