@extends('layouts.app')
@section('title','أطباء التنويم')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="d-flex justify-content-between mb-3"><div><h1 class="h4">أطباء التنويم</h1><p class="text-muted">قائمة الأطباء المستخدمة في الموافقات الطبية والحاسبة.</p></div><a class="btn btn-primary" href="{{ route('modules.admission-inpatient.doctors.create') }}">إضافة طبيب</a></div><form class="mb-3"><div class="row g-2"><div class="col-md-8"><input class="form-control" name="search" value="{{ $search }}" placeholder="بحث بالمعرف أو الاسم أو البريد أو الجوال"></div><div class="col-md-2"><select class="form-select" name="status"><option value="">كل الحالات</option><option value="active" @selected($status==='active')>نشط</option><option value="inactive" @selected($status==='inactive')>غير نشط</option></select></div><div class="col-md-2"><button class="btn btn-outline-primary w-100">بحث</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>المعرف</th><th>الاسم</th><th>التخصص/المسمى</th><th>البريد</th><th>الجوال</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($doctors as $row)<tr><td>{{ $row->doctor_id_no }}</td><td>{{ $row->name_ar ?: $row->name_en }}</td><td>{{ $row->designation_ar ?: $row->designation_en }}</td><td>{{ $row->email }}</td><td>{{ $row->mobile_no }}</td><td>{{ $row->status }}</td><td class="text-nowrap"><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-inpatient.doctors.edit',$row->id) }}">تعديل</a><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.doctors.toggle',$row->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">تفعيل/إيقاف</button></form><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.doctors.destroy',$row->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@empty<tr><td colspan="7" class="text-center py-5 text-muted">لا يوجد أطباء.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $doctors->links() }}</div></div></div>@endsection
