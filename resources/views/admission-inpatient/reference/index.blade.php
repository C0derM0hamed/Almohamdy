@extends('layouts.app')
@section('title', $spec['title'])
@section('content')
<div class="container-fluid py-3" dir="rtl"><div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h4">{{ $spec['title'] }}</h1><p class="text-muted">إدارة البيانات المرجعية المستخدمة في التنويم والحاسبة.</p></div><a class="btn btn-primary" href="{{ route('modules.admission-inpatient.reference.create', $type) }}">إضافة جديد</a></div>
<form class="card border-0 shadow-sm mb-3"><div class="card-body row g-2"><div class="col-md-6"><input class="form-control" name="search" value="{{ $search }}" placeholder="بحث"></div>@if(in_array($type,['rooms','service-prices'],true))<div class="col-md-4"><select class="form-select" name="status_id"><option value="">كل حالات التنويم</option>@foreach($options['statuses'] ?? [] as $status)<option value="{{ $status->id }}" @selected((string)request('status_id') === (string)$status->id)>{{ $status->name_ar ?: $status->name_en }}</option>@endforeach</select></div>@endif<div class="col-md-2"><button class="btn btn-outline-primary w-100">بحث</button></div></div></form>
@if($type === 'service-prices')<form class="card border-0 shadow-sm mb-3" method="post" enctype="multipart/form-data" action="{{ route('modules.admission-inpatient.reference.import',$type) }}"><div class="card-body row g-2 align-items-end">@csrf<div class="col-md-4"><label class="form-label">استيراد ملف XLSX/CSV</label><input class="form-control" type="file" name="file" accept=".xlsx,.csv,.txt" required></div><div class="col-md-4"><label class="form-label">حالة التنويم</label><select class="form-select" name="status_id" required><option value="">اختر</option>@foreach($options['statuses'] ?? [] as $status)<option value="{{ $status->id }}">{{ $status->name_ar ?: $status->name_en }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-primary w-100">استيراد الأسعار</button></div></div></form>@endif
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>#</th>@foreach($spec['fields'] as $field)<th>{{ $spec['labels'][$field] ?? $field }}</th>@endforeach<th>الحالة</th><th></th></tr></thead><tbody>@forelse($rows as $row)<tr><td>{{ $row->id }}</td>@foreach($spec['fields'] as $field)<td>{{ $row->{$field} ?? '' }}</td>@endforeach<td>@if(isset($row->publish))<span class="badge text-bg-{{ $row->publish ? 'success':'secondary' }}">{{ $row->publish ? 'مفعل':'غير مفعل' }}</span>@endif</td><td class="text-nowrap"><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-inpatient.reference.edit',[$type,$row->id]) }}">تعديل</a>@if(isset($row->publish))<form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.reference.toggle',[$type,$row->id]) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">تفعيل/إيقاف</button></form>@endif<form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.reference.destroy',[$type,$row->id]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@empty<tr><td colspan="{{ count($spec['fields'])+3 }}" class="text-center py-5 text-muted">لا توجد سجلات.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $rows->links() }}</div></div></div>
@endsection
