@extends('layouts.app')
@section('title',$row ? 'تعديل جهة اتصال':'إضافة جهة اتصال')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="card border-0 shadow-sm"><div class="card-body"><h1 class="h4 mb-4">{{ $row ? 'تعديل جهة اتصال':'إضافة جهة اتصال' }}</h1><form method="post" action="{{ $row ? route('modules.admission-inpatient.contacts.update',[$kind,$row->id]) : route('modules.admission-inpatient.contacts.store',$kind) }}">@csrf @if($row) @method('PUT') @endif<div class="row g-3"><div class="col-md-4"><label class="form-label">اسم الموظف</label><input class="form-control" name="employee_name" required value="{{ old('employee_name',$row->employee_name ?? '') }}"></div><div class="col-md-4"><label class="form-label">البريد</label><input class="form-control" type="email" name="email" value="{{ old('email',$row->email ?? '') }}"></div><div class="col-md-4"><label class="form-label">الجوال</label><input class="form-control" name="mobile" value="{{ old('mobile',$row->mobile ?? '') }}"></div><div class="col-md-4"><label class="form-label">الحالة</label><select class="form-select" name="status"><option value="active">نشط</option><option value="inactive">غير نشط</option></select></div></div><button class="btn btn-primary mt-4">حفظ</button></form></div></div></div>@endsection
