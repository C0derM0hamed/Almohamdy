@extends('layouts.app')
@section('title',$kind === 'cc' ? 'نسخ الموافقات الطبية':'جهات التحصيل')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="d-flex justify-content-between mb-3"><h1 class="h4">{{ $kind === 'cc' ? 'جهات نسخ الموافقات الطبية':'جهات قسم التحصيل' }}</h1><a class="btn btn-primary" href="{{ route('modules.admission-inpatient.contacts.create',$kind) }}">إضافة جهة</a></div><ul class="nav nav-tabs mb-3"><li class="nav-item"><a class="nav-link {{ $kind === 'cc' ? 'active' : '' }}" href="{{ route('modules.admission-inpatient.contacts.index','cc') }}">نسخ الموافقات</a></li><li class="nav-item"><a class="nav-link {{ $kind === 'collections' ? 'active' : '' }}" href="{{ route('modules.admission-inpatient.contacts.index','collections') }}">قسم التحصيل</a></li></ul><form class="mb-3"><div class="input-group"><input class="form-control" name="search" value="{{ $search }}" placeholder="بحث"><button class="btn btn-outline-primary">بحث</button></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>الموظف</th><th>البريد</th><th>الجوال</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($contacts as $row)<tr><td>{{ $row->employee_name }}</td><td>{{ $row->email }}</td><td>{{ $row->mobile }}</td><td>{{ $row->status }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-inpatient.contacts.edit',[$kind,$row->id]) }}">تعديل</a><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.contacts.toggle',[$kind,$row->id]) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">تفعيل/إيقاف</button></form><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.contacts.destroy',[$kind,$row->id]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-5 text-muted">لا توجد جهات.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $contacts->links() }}</div></div></div>@endsection
