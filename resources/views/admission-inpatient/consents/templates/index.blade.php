@extends('layouts.app')
@section('title','قوالب إقرارات التنويم')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="d-flex justify-content-between mb-3"><h1 class="h4">قوالب إقرارات التنويم</h1><a class="btn btn-primary" href="{{ route('modules.admission-inpatient.consent-templates.create') }}">قالب جديد</a></div><form class="mb-3"><div class="input-group"><input class="form-control" name="search" value="{{ $search }}" placeholder="بحث"><button class="btn btn-outline-primary">بحث</button></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>العنوان</th><th>النص</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($templates as $row)<tr><td>{{ $row->title }}</td><td>{{ \Illuminate\Support\Str::limit($row->consent_content,160) }}</td><td>{{ $row->publish ? 'مفعل':'غير مفعل' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-inpatient.consent-templates.edit',$row->id) }}">تعديل</a><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.consent-templates.toggle',$row->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">تفعيل/إيقاف</button></form><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.consent-templates.destroy',$row->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-muted">لا توجد قوالب.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $templates->links() }}</div></div></div>@endsection
