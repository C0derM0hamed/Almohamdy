@extends('layouts.app')
@section('title',$row ? 'تعديل قالب الإقرار':'قالب إقرار جديد')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="card border-0 shadow-sm"><div class="card-body"><h1 class="h4 mb-4">{{ $row ? 'تعديل قالب الإقرار':'قالب إقرار جديد' }}</h1><form method="post" action="{{ $row ? route('modules.admission-inpatient.consent-templates.update',$row->id) : route('modules.admission-inpatient.consent-templates.store') }}">@csrf @if($row) @method('PUT') @endif<label class="form-label">العنوان</label><input class="form-control mb-3" name="title" required value="{{ old('title',$row->title ?? '') }}"><label class="form-label">نص الإقرار</label><textarea class="form-control mb-3" rows="12" name="consent_content" required>{{ old('consent_content',$row->consent_content ?? '') }}</textarea><div class="form-check mb-3"><input class="form-check-input" type="checkbox" name="publish" value="1" id="publish" @checked(old('publish',$row->publish ?? 1))><label class="form-check-label" for="publish">مفعل</label></div><button class="btn btn-primary">حفظ</button></form></div></div></div>@endsection
