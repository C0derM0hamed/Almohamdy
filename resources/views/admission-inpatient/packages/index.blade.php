@extends('layouts.app')
@section('title','حزم التنويم')
@section('content')<div class="container-fluid py-3" dir="rtl"><div class="d-flex justify-content-between mb-3"><div><h1 class="h4">حزم التنويم</h1><p class="text-muted">الحزم المرتبطة بالعيادات وشركات التأمين.</p></div><a class="btn btn-primary" href="{{ route('modules.admission-inpatient.packages.create') }}">حزمة جديدة</a></div><form class="card border-0 shadow-sm mb-3"><div class="card-body row g-2"><div class="col-md-6"><input class="form-control" name="search" value="{{ $filters['search'] }}" placeholder="بحث بالكود أو الاسم"></div><div class="col-md-2"><button class="btn btn-outline-primary w-100">بحث</button></div></div></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>الكود</th><th>الاسم</th><th>الأيام</th><th>السعر</th><th>الحالة</th><th></th></tr></thead><tbody>@forelse($packages as $row)<tr><td>{{ $row->code }}</td><td>{{ $row->name_ar ?: $row->name_en }}</td><td>{{ $row->days }}</td><td>{{ number_format((float)$row->price,2) }}</td><td>{{ $row->publish ? 'مفعل':'غير مفعل' }}</td><td><a class="btn btn-sm btn-outline-secondary" href="{{ route('modules.admission-inpatient.packages.pdf',$row->id) }}">PDF</a><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.admission-inpatient.packages.edit',$row->id) }}">تعديل</a><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.packages.toggle',$row->id) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-outline-secondary">تفعيل</button></form><form class="d-inline" method="post" action="{{ route('modules.admission-inpatient.packages.destroy',$row->id) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">حذف</button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5 text-muted">لا توجد حزم.</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $packages->links() }}</div></div></div>@endsection
