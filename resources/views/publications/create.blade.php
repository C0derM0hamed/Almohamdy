@extends('layouts.app')
@section('title', __('publications.create'))
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="h4 mb-0">{{ __('publications.create') }}</h1><a class="btn btn-outline-secondary" href="{{ route('modules.publications.index') }}"><i class="bi bi-arrow-right"></i> {{ __('publications.back') }}</a></div>
<form class="card border-0 shadow-sm" method="post" action="{{ route('modules.publications.store') }}" enctype="multipart/form-data">@csrf<div class="card-body row g-3"><div class="col-md-6"><label class="form-label">{{ __('publications.branch') }} *</label>@foreach($branches as $branch)<div class="form-check"><input class="form-check-input" type="checkbox" name="branch_ids[]" value="{{ $branch->id }}" checked><label class="form-check-label">{{ $branch->name_ar }}</label></div>@endforeach</div><div class="col-md-6"><label class="form-label">{{ __('publications.type') }} *</label><select class="form-select" name="post_type_id" required>@foreach($types as $type)<option value="{{ $type->id }}">{{ $type->name_ar }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">{{ __('publications.subject_ar') }} *</label><input class="form-control" name="subject_ar" required></div><div class="col-md-6"><label class="form-label">{{ __('publications.subject_en') }}</label><input class="form-control" name="subject_en"></div><div class="col-md-6"><label class="form-label">{{ __('publications.body_ar') }} *</label><textarea class="form-control" rows="6" name="post_ar" required></textarea></div><div class="col-md-6"><label class="form-label">{{ __('publications.body_en') }}</label><textarea class="form-control" rows="6" name="post_en"></textarea></div><div class="col-md-6"><label class="form-label">{{ __('publications.attachment') }}</label><input class="form-control" type="file" name="uploaded_file"></div></div><div class="card-footer text-start"><button class="btn btn-primary"><i class="bi bi-check2"></i> {{ __('publications.save') }}</button></div></form>
@endsection
