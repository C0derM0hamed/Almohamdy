@extends('layouts.app')
@section('title', __('publications.title'))
@section('content')
<div class="d-flex flex-wrap justify-content-between align-items-end gap-3 mb-4"><div><h1 class="h4 mb-1">{{ __('publications.title') }}</h1><p class="text-muted mb-0">{{ __('publications.scope') }}</p></div><a class="btn btn-primary" href="{{ route('modules.publications.create') }}"><i class="bi bi-plus-lg"></i> {{ __('publications.create') }}</a></div>
<form class="card border-0 shadow-sm mb-3" method="get"><div class="card-body row g-3 align-items-end"><div class="col-md-6"><label class="form-label">{{ __('publications.search') }}</label><input class="form-control" name="search" value="{{ $filters['search'] }}"></div><div class="col-md-4"><label class="form-label">{{ __('publications.type') }}</label><select class="form-select" name="type_id"><option value="">{{ __('publications.all') }}</option>@foreach($types as $type)<option value="{{ $type->id }}" @selected($filters['type_id'] === $type->id)>{{ $type->name_ar }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100"><i class="bi bi-search"></i> {{ __('publications.search') }}</button></div></div></form>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>#</th><th>{{ __('publications.subject') }}</th><th>{{ __('publications.type') }}</th><th>{{ __('publications.branch') }}</th><th>{{ __('publications.date') }}</th><th></th></tr></thead><tbody>@forelse($posts as $post)<tr><td>{{ $post->id }}</td><td>{{ $post->subject_ar }}</td><td>{{ $post->type_name_ar }}</td><td>{{ $post->branch_name_ar }}</td><td>{{ $post->created_at ?? '' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('modules.publications.show', $post->id) }}" title="{{ __('publications.open') }}"><i class="bi bi-eye"></i></a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">{{ __('publications.empty') }}</td></tr>@endforelse</tbody></table></div><div class="p-3">{{ $posts->links() }}</div></div>
@endsection
